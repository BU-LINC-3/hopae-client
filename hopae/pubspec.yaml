name: hopae
description: hopae-client
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  retrofit: ^2.0.1
  json_serializable: ^5.0.0
  cupertino_icons: ^1.0.3
  shared_preferences: ^2.0.6
  qr_flutter: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  retrofit_generator: ^2.0.1
  build_runner: ^2.0.6

flutter:
  uses-material-design: true
